{
  "name": "smart-spend",
  "version": "1.0.0",
  "description": "AI-powered expense tracking and budget management application",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && npm start",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "seed": "cd backend && node scripts/seedBudgets.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["expense", "budget", "finance", "ai", "tracking"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
